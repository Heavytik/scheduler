Small React + express project.

Aim is to make daily scheduler, like timetable for everyday life (routines).

You can make daily schedule with events.

Events contains starting time, description and image.

Program show image with description, progress bar and time until next event (and what happens next)

App is good for example kids (and parents) to see what (should be) is going on. 